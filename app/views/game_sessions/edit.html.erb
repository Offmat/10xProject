<% content_for :title, 'Edit Session' %>

<div class="mx-auto max-w-2xl space-y-6">
  <div>
    <h1 class="text-2xl font-bold">Edit Session</h1>
    <p class="mt-1 text-base-content/70">Update session details. Changed participants will be re-notified.</p>
  </div>

  <%= render 'form' %>
</div>
